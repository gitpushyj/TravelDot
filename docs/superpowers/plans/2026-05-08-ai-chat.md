# AI 여행 친구 (AI 탭) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** VisitGrid의 AI 탭에서 사용자의 여행 데이터·프로필을 컨텍스트로 받은 LLM이 친구체로 답하는 채팅 화면을 구현한다.

**Architecture:** 클라(RN) → Supabase Edge Function `ai-chat` → OpenAI Responses API. 키는 Supabase secret에. 클라 메시지 메모리는 AsyncStorage 10개 sliding window. 사용량은 Supabase 테이블로 KST 자정 리셋.

**Tech Stack:** React Native(Expo 54), Zustand, AsyncStorage, Supabase JS v2, Supabase Edge Functions(Deno), OpenAI Responses API(`gpt-5.4-mini`, `web_search` tool), i18next.

**Spec:** [docs/superpowers/specs/2026-05-08-ai-chat-design.md](../specs/2026-05-08-ai-chat-design.md)

**Tier policy reference:** [docs/user-tier.md](../../user-tier.md)

---

## File Structure (작업 전 전체 그림)

**신규 (server)**
```
supabase/
  config.toml                                  # supabase CLI 인식용 최소 설정
  migrations/
    20260508120000_ai_chat_usage.sql
  functions/
    ai-chat/
      index.ts                                  # 진입점
      systemPrompt.ts                           # system prompt 조립 (pure)
      tripStats.ts                              # trips → country stats (pure)
      tripStats.test.ts                         # Deno test
      systemPrompt.test.ts                      # Deno test
      deno.jsonc                                # imports 정리
```

**신규 (client)**
```
src/features/aiChat/
  types.ts
  parseAssistantText.ts
  parseAssistantText.test.ts
  aiChatStorage.ts
  aiChatClient.ts
  aiChatStore.ts

src/components/AiChat/
  AiChatBubble.tsx
  AiChatList.tsx
  AiChatComposer.tsx
  AiChatEmptyState.tsx
  UsageLimitBanner.tsx
  index.ts
```

**수정**
```
src/screens/AiScreen.tsx                        # placeholder → 실제 화면
src/features/auth/authStore.ts                  # signOut에서 AI 채팅 메모리 정리
src/i18n/locales/ko.json                        # aiChat.* 친구체 ko
src/i18n/locales/en.json                        # aiChat.* 친구체 en
src/i18n/locales/{ja,zh-CN,zh-TW,es,de,fr,it,ru}.json  # aiChat.* 영어값으로 채움
docs/배포-체크리스트.md                          # OPENAI_API_KEY 등 운영 항목 추가
```

---

## Phase A: 서버 측 (DB + Edge Function)

### Task A1: supabase 디렉터리 셋업 + `ai_chat_usage` 마이그레이션

**Files:**
- Create: `supabase/config.toml`
- Create: `supabase/migrations/20260508120000_ai_chat_usage.sql`

**왜:** 사용자가 `supabase` CLI로 `db push` / `functions deploy` 할 수 있는 표준 디렉터리 구조를 만든다. `config.toml`은 CLI가 프로젝트 루트로 인식하기 위한 최소 stub.

- [ ] **Step 1: `supabase/config.toml` 생성 (최소 stub)**

```toml
# supabase CLI가 프로젝트 루트로 인식하기 위한 최소 설정.
# 실제 원격 프로젝트 ref는 supabase link 시 .branches에 저장된다.
project_id = "visitgrid"
```

- [ ] **Step 2: 마이그레이션 SQL 작성**

`supabase/migrations/20260508120000_ai_chat_usage.sql`:

```sql
-- AI 채팅 일일 사용량 카운터.
-- KST 자정 기준 day로 그룹. service_role(Edge Function)만 변경, 클라는 본인 행 select만 가능.

create table public.ai_chat_usage (
  user_id uuid not null references auth.users(id) on delete cascade,
  day_kst date not null,
  count integer not null default 0,
  updated_at timestamptz not null default now(),
  primary key (user_id, day_kst)
);

alter table public.ai_chat_usage enable row level security;

create policy ai_chat_usage_select_self
  on public.ai_chat_usage for select
  using (auth.uid() = user_id);

-- insert/update는 정책 없음 → service_role만 가능.

create index ai_chat_usage_user_day_idx
  on public.ai_chat_usage (user_id, day_kst desc);
```

- [ ] **Step 3: 커밋**

```bash
git add supabase/config.toml supabase/migrations/20260508120000_ai_chat_usage.sql
git commit -m "feat(ai-chat): supabase 마이그레이션 - ai_chat_usage 테이블 + RLS"
```

---

### Task A2: `tripStats.ts` (순수 집계) + 테스트 (Deno)

**Files:**
- Create: `supabase/functions/ai-chat/tripStats.ts`
- Create: `supabase/functions/ai-chat/tripStats.test.ts`

**왜:** trips 행 배열을 받아 (a) country별 stats top 30, (b) start_date desc 정렬된 trips 배열을 만드는 순수 함수. 테스트로 잠긴 다음에 index.ts에서 호출만 한다.

- [ ] **Step 1: 테스트 먼저**

`tripStats.test.ts`:

```ts
import { assertEquals } from "https://deno.land/std@0.224.0/assert/mod.ts";
import { aggregateCountryStats, sortTripsForPrompt } from "./tripStats.ts";

const sample = [
  { country_code: "JP", start_date: "2024-08-12", end_date: "2024-08-19", body: "오사카" },
  { country_code: "JP", start_date: "2023-04-01", end_date: "2023-04-05", body: null },
  { country_code: "TH", start_date: "2024-07-03", end_date: "2024-07-05", body: "" },
  { country_code: "TH", start_date: "2022-12-30", end_date: "2023-01-02", body: "송끄란 전" }, // 4 days inclusive
];

Deno.test("aggregateCountryStats - groups by country with correct stats", () => {
  const stats = aggregateCountryStats(sample);
  // 방문 횟수 desc, 동률이면 코드 asc
  assertEquals(stats.length, 2);
  // JP: 2 visits, first 2023-04-01, last 2024-08-12, days = (5-1+1)+(19-12+1) = 5+8 = 13
  assertEquals(stats[0], {
    code: "JP",
    visits: 2,
    first_visit: "2023-04-01",
    last_visit: "2024-08-12",
    total_days: 13,
  });
  // TH: 2 visits, first 2022-12-30, last 2024-07-03, days = (Jan2-Dec30+1)+(5-3+1) = 4+3 = 7
  assertEquals(stats[1], {
    code: "TH",
    visits: 2,
    first_visit: "2022-12-30",
    last_visit: "2024-07-03",
    total_days: 7,
  });
});

Deno.test("aggregateCountryStats - top 30 by visits desc, tie by code asc", () => {
  const rows = Array.from({ length: 35 }, (_, i) => ({
    country_code: String.fromCharCode(65 + (i % 26)) + String.fromCharCode(65 + ((i + 1) % 26)),
    start_date: "2024-01-01",
    end_date: "2024-01-01",
    body: null,
  }));
  const stats = aggregateCountryStats(rows);
  assertEquals(stats.length <= 30, true);
});

Deno.test("sortTripsForPrompt - start_date desc, deterministic", () => {
  const sorted = sortTripsForPrompt(sample);
  assertEquals(sorted.map((t) => t.start_date), [
    "2024-08-12",
    "2024-07-03",
    "2023-04-01",
    "2022-12-30",
  ]);
});

Deno.test("aggregateCountryStats - same-day trip counts as 1 day", () => {
  const stats = aggregateCountryStats([
    { country_code: "FR", start_date: "2024-05-01", end_date: "2024-05-01", body: null },
  ]);
  assertEquals(stats[0].total_days, 1);
});

Deno.test("aggregateCountryStats - empty input returns []", () => {
  assertEquals(aggregateCountryStats([]), []);
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd supabase/functions/ai-chat && deno test tripStats.test.ts`
Expected: 모듈 없음 에러로 FAIL.

> **Note:** Deno가 시스템에 없으면 supabase CLI에 번들된 deno를 쓸 수 있다(`supabase functions deno test ...`). 사용자가 둘 다 없으면 step 4의 통합 검증 시점까지 미루고, 코드 리뷰만 진행해도 된다. Plan은 deno 가용 가정으로 진행.

- [ ] **Step 3: `tripStats.ts` 구현**

```ts
// trips 행 → AI system prompt에 넣을 통계로 변환하는 순수 함수.
// 외부 의존 없음. 테스트는 Deno test로.

export type TripRow = {
  country_code: string;
  start_date: string; // YYYY-MM-DD
  end_date: string;   // YYYY-MM-DD
  body: string | null;
};

export type CountryStat = {
  code: string;
  visits: number;
  first_visit: string;
  last_visit: string;
  total_days: number;
};

const MS_PER_DAY = 86_400_000;

function inclusiveDays(start: string, end: string): number {
  const s = Date.parse(start + "T00:00:00Z");
  const e = Date.parse(end + "T00:00:00Z");
  if (Number.isNaN(s) || Number.isNaN(e)) return 0;
  return Math.max(1, Math.round((e - s) / MS_PER_DAY) + 1);
}

export function aggregateCountryStats(rows: TripRow[]): CountryStat[] {
  const acc = new Map<string, CountryStat>();
  for (const r of rows) {
    const cur = acc.get(r.country_code);
    const days = inclusiveDays(r.start_date, r.end_date);
    if (!cur) {
      acc.set(r.country_code, {
        code: r.country_code,
        visits: 1,
        first_visit: r.start_date,
        last_visit: r.start_date,
        total_days: days,
      });
    } else {
      cur.visits += 1;
      if (r.start_date < cur.first_visit) cur.first_visit = r.start_date;
      if (r.start_date > cur.last_visit) cur.last_visit = r.start_date;
      cur.total_days += days;
    }
  }
  // 방문 횟수 desc, 동률이면 코드 asc
  const sorted = Array.from(acc.values()).sort((a, b) =>
    b.visits - a.visits || (a.code < b.code ? -1 : a.code > b.code ? 1 : 0)
  );
  return sorted.slice(0, 30);
}

export function sortTripsForPrompt(rows: TripRow[]): TripRow[] {
  return [...rows].sort((a, b) =>
    a.start_date < b.start_date ? 1 : a.start_date > b.start_date ? -1 : 0
  );
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd supabase/functions/ai-chat && deno test tripStats.test.ts`
Expected: 5 passed, 0 failed.

- [ ] **Step 5: 커밋**

```bash
git add supabase/functions/ai-chat/tripStats.ts supabase/functions/ai-chat/tripStats.test.ts
git commit -m "feat(ai-chat): tripStats 집계 순수 함수 + Deno test"
```

---

### Task A3: `systemPrompt.ts` (조립) + 테스트

**Files:**
- Create: `supabase/functions/ai-chat/systemPrompt.ts`
- Create: `supabase/functions/ai-chat/systemPrompt.test.ts`

**왜:** stats + trips + 프로필 메타를 받아 spec §4.4의 텍스트를 생성하는 순수 함수. 50자 truncate, 빈 메모 생략, 0건 처리까지 여기서.

- [ ] **Step 1: 테스트 작성**

```ts
import { assertEquals, assertStringIncludes } from "https://deno.land/std@0.224.0/assert/mod.ts";
import { buildSystemPrompt, truncateMemo } from "./systemPrompt.ts";

Deno.test("truncateMemo - <= 50 codepoints returns as-is", () => {
  assertEquals(truncateMemo("짧은 메모"), "짧은 메모");
  assertEquals(truncateMemo(""), "");
});

Deno.test("truncateMemo - > 50 codepoints adds ellipsis", () => {
  const long = "가".repeat(60);
  const out = truncateMemo(long);
  assertEquals([...out].length, 51); // 50 + ellipsis
  assertEquals(out.endsWith("…"), true);
});

Deno.test("truncateMemo - emoji boundary safe", () => {
  // 🇰🇷 is 2 codepoints (regional indicator pair). 25 of them = 50 codepoints, fits exactly.
  const emoji = "🇰🇷".repeat(25);
  assertEquals(truncateMemo(emoji), emoji);
});

Deno.test("buildSystemPrompt - empty trips → has-no-trips line", () => {
  const out = buildSystemPrompt({
    lang: "ko",
    authProvider: "google",
    tier: "free",
    stats: [],
    trips: [],
  });
  assertStringIncludes(out, "USER has no trips yet.");
});

Deno.test("buildSystemPrompt - includes profile, stats and trips sections", () => {
  const out = buildSystemPrompt({
    lang: "en",
    authProvider: "apple",
    tier: "premium",
    stats: [
      { code: "JP", visits: 2, first_visit: "2023-04-01", last_visit: "2024-08-12", total_days: 13 },
    ],
    trips: [
      { country_code: "JP", start_date: "2024-08-12", end_date: "2024-08-19", body: "오사카 다코야키 투어 — 첫째 날부터 비가 와서 정말 힘들었지만 결국 맛집은 다 갔다" },
      { country_code: "JP", start_date: "2023-04-01", end_date: "2023-04-05", body: null },
    ],
  });
  assertStringIncludes(out, "auth provider: apple");
  assertStringIncludes(out, "account tier: premium");
  assertStringIncludes(out, "app language: en");
  assertStringIncludes(out, "JP");
  assertStringIncludes(out, "2024-08-12 ~ 2024-08-19");
  // 50자 truncate + 말줄임표
  assertStringIncludes(out, "…");
  // null body는 따옴표 블록 자체가 없어야
  const apr1Line = out.split("\n").find((l) => l.includes("2023-04-01"));
  assertEquals(apr1Line?.includes('"'), false);
});

Deno.test("buildSystemPrompt - friend tone instructions present", () => {
  const out = buildSystemPrompt({
    lang: "ko",
    authProvider: "google",
    tier: "free",
    stats: [],
    trips: [],
  });
  assertStringIncludes(out, "close friend");
  assertStringIncludes(out, "casual");
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd supabase/functions/ai-chat && deno test systemPrompt.test.ts`
Expected: 모듈 없음 FAIL.

- [ ] **Step 3: 구현**

```ts
import type { CountryStat, TripRow } from "./tripStats.ts";

export type TierName = "free" | "premium" | "power";
export type AuthProvider = "google" | "apple" | "unknown";

export type SystemPromptInput = {
  lang: string;
  authProvider: AuthProvider;
  tier: TierName;
  stats: CountryStat[]; // already top-30 sorted
  trips: TripRow[];     // already start_date desc, capped to 200 by caller
};

const MEMO_MAX_CODEPOINTS = 50;

export function truncateMemo(memo: string): string {
  const arr = Array.from(memo);
  if (arr.length <= MEMO_MAX_CODEPOINTS) return memo;
  return arr.slice(0, MEMO_MAX_CODEPOINTS).join("") + "…";
}

function statsTable(stats: CountryStat[]): string {
  if (stats.length === 0) return "(no country stats yet)";
  const header =
    "| code | visits | first_visit | last_visit | total_days |\n" +
    "|------|--------|-------------|------------|------------|";
  const rows = stats.map(
    (s) =>
      `| ${s.code} | ${s.visits} | ${s.first_visit} | ${s.last_visit} | ${s.total_days} |`
  );
  return [header, ...rows].join("\n");
}

function tripsList(trips: TripRow[]): string {
  if (trips.length === 0) return "USER has no trips yet.";
  return trips
    .map((t) => {
      const memo = (t.body ?? "").trim();
      if (!memo) return `- ${t.start_date} ~ ${t.end_date}  ${t.country_code}`;
      return `- ${t.start_date} ~ ${t.end_date}  ${t.country_code}  "${truncateMemo(memo)}"`;
    })
    .join("\n");
}

export function buildSystemPrompt(input: SystemPromptInput): string {
  return [
    "You are VisitGrid's personal travel companion for this user.",
    `Reply in the user's app language: ${input.lang}.`,
    "Talk to the user like a close friend (casual, warm, second-person).",
    "Match the casual register of their app language.",
    "Be concise and grounded in the user's own travel history.",
    "Use the web_search tool ONLY when the question depends on time-sensitive,",
    "real-world facts (current safety/geopolitics, visa rules, weather windows,",
    "prices). Do NOT search for general advice the user already implies.",
    "When you reference a place that benefits from a photo or map image, you",
    "MAY include up to 4 https image URLs in your reply as markdown",
    "(`![alt](https://...)`). The client renders them inline.",
    "",
    "USER PROFILE",
    `- auth provider: ${input.authProvider}`,
    `- app language: ${input.lang}`,
    `- account tier: ${input.tier}`,
    "",
    "USER COUNTRY STATS  (top 30 by visit count)",
    statsTable(input.stats),
    "",
    "TRIPS  (all, up to 200, newest first; memo truncated to 50 chars)",
    tripsList(input.trips),
  ].join("\n");
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd supabase/functions/ai-chat && deno test systemPrompt.test.ts`
Expected: 6 passed, 0 failed.

- [ ] **Step 5: 커밋**

```bash
git add supabase/functions/ai-chat/systemPrompt.ts supabase/functions/ai-chat/systemPrompt.test.ts
git commit -m "feat(ai-chat): systemPrompt 조립 + 50자 codepoint truncate + Deno test"
```

---

### Task A4: Edge Function `index.ts` (요청 처리/한도/OpenAI 호출)

**Files:**
- Create: `supabase/functions/ai-chat/index.ts`
- Create: `supabase/functions/ai-chat/deno.jsonc`

**왜:** spec §6.3 흐름을 그대로 구현. 단위 테스트 어렵기 때문에 Edge Function을 deploy 후 통합 검증으로 간다.

- [ ] **Step 1: `deno.jsonc` 생성**

```jsonc
{
  "imports": {
    "supabase": "https://esm.sh/@supabase/supabase-js@2.45.0"
  }
}
```

- [ ] **Step 2: `index.ts` 구현**

```ts
// AI 채팅 Edge Function.
// - JWT 검증 → user_id
// - users.tier → 일일 한도
// - ai_chat_usage 카운터 검사 (KST 자정 기준)
// - trips 200건 조회 → systemPrompt 조립
// - OpenAI Responses API 호출 (web_search tool)
// - 성공 시에만 카운트 +1

import { createClient } from "supabase";
import { aggregateCountryStats, sortTripsForPrompt, type TripRow } from "./tripStats.ts";
import { buildSystemPrompt, type AuthProvider, type TierName } from "./systemPrompt.ts";

type IncomingMessage = { role: "user" | "assistant"; text: string };
type IncomingBody = { messages: IncomingMessage[] };

const TIER_LIMITS: Record<TierName, number> = {
  free: 1,
  premium: 10,
  power: 30,
};

const TIER_NAMES: Record<number, TierName> = { 0: "free", 1: "premium", 2: "power" };

const OPENAI_MODEL = Deno.env.get("OPENAI_MODEL") ?? "gpt-5.4-mini";
const OPENAI_API_KEY = Deno.env.get("OPENAI_API_KEY");
const SUPABASE_URL = Deno.env.get("SUPABASE_URL")!;
const SERVICE_ROLE = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!;
const ANON_KEY = Deno.env.get("SUPABASE_ANON_KEY")!;

function json(body: unknown, status = 200): Response {
  return new Response(JSON.stringify(body), {
    status,
    headers: { "content-type": "application/json" },
  });
}

function todayKst(): string {
  const kstMs = Date.now() + 9 * 60 * 60 * 1000;
  return new Date(kstMs).toISOString().slice(0, 10);
}

function tierFromNumber(n: number | null | undefined): TierName {
  if (n == null) return "free";
  return TIER_NAMES[n] ?? "free";
}

function authProviderFromUser(user: { app_metadata?: { provider?: string } }): AuthProvider {
  const p = user.app_metadata?.provider;
  if (p === "google") return "google";
  if (p === "apple") return "apple";
  return "unknown";
}

// Responses API 응답에서 텍스트만 안전하게 추출.
// 응답 schema는 tool 호출 등이 섞일 수 있어 output[].content[].text를 모은다.
type ResponsesContent = { type?: string; text?: string };
type ResponsesOutputItem = { type?: string; content?: ResponsesContent[] };
type ResponsesPayload = { output_text?: string; output?: ResponsesOutputItem[] };

function extractText(payload: ResponsesPayload): string {
  if (typeof payload.output_text === "string" && payload.output_text.length > 0) {
    return payload.output_text;
  }
  const parts: string[] = [];
  for (const item of payload.output ?? []) {
    for (const c of item.content ?? []) {
      if (typeof c.text === "string") parts.push(c.text);
    }
  }
  return parts.join("\n").trim();
}

Deno.serve(async (req) => {
  if (req.method !== "POST") return json({ error: "method_not_allowed" }, 405);

  const auth = req.headers.get("Authorization") ?? "";
  const jwt = auth.startsWith("Bearer ") ? auth.slice(7) : "";
  if (!jwt) return json({ error: "unauthorized" }, 401);

  // 1) JWT → user
  const userClient = createClient(SUPABASE_URL, ANON_KEY, {
    global: { headers: { Authorization: `Bearer ${jwt}` } },
  });
  const { data: userRes, error: userErr } = await userClient.auth.getUser(jwt);
  if (userErr || !userRes.user) return json({ error: "unauthorized" }, 401);
  const user = userRes.user;

  // 2) Body 검증
  let body: IncomingBody;
  try {
    body = await req.json();
  } catch {
    return json({ error: "invalid_input", reason: "bad_json" }, 400);
  }
  const messages = Array.isArray(body?.messages) ? body.messages : [];
  if (messages.length < 1 || messages.length > 11) {
    return json({ error: "invalid_input", reason: "messages_count" }, 400);
  }
  for (const m of messages) {
    if (m.role !== "user" && m.role !== "assistant") {
      return json({ error: "invalid_input", reason: "role" }, 400);
    }
    if (typeof m.text !== "string" || m.text.length === 0 || m.text.length > 500) {
      return json({ error: "invalid_input", reason: "text_length" }, 400);
    }
  }
  if (messages[messages.length - 1].role !== "user") {
    return json({ error: "invalid_input", reason: "last_role" }, 400);
  }

  // 3) tier 조회 (service_role)
  const adminClient = createClient(SUPABASE_URL, SERVICE_ROLE);
  const { data: userRow } = await adminClient
    .from("users")
    .select("tier")
    .eq("id", user.id)
    .maybeSingle();
  const tier = tierFromNumber(userRow?.tier as number | null | undefined);
  const limit = TIER_LIMITS[tier];

  // 4) 일일 카운트 조회
  const day = todayKst();
  const { data: usageRow } = await adminClient
    .from("ai_chat_usage")
    .select("count")
    .eq("user_id", user.id)
    .eq("day_kst", day)
    .maybeSingle();
  const usedToday = (usageRow?.count as number | undefined) ?? 0;

  if (usedToday >= limit) {
    return json({ error: "rate_limited", tier, limit }, 429);
  }

  // 5) trips 200건
  const { data: tripRows } = await adminClient
    .from("trips")
    .select("country_code,start_date,end_date,body")
    .eq("user_id", user.id)
    .is("deleted_at", null)
    .order("start_date", { ascending: false })
    .limit(200);
  const trips = (tripRows ?? []) as TripRow[];

  // 6) systemPrompt
  const lang = (user.user_metadata?.lang as string | undefined) ?? "en";
  const systemPrompt = buildSystemPrompt({
    lang,
    authProvider: authProviderFromUser(user),
    tier,
    stats: aggregateCountryStats(trips),
    trips: sortTripsForPrompt(trips),
  });

  // 7) OpenAI Responses API
  if (!OPENAI_API_KEY) return json({ error: "internal" }, 500);

  const openaiInput = [
    { role: "system" as const, content: systemPrompt },
    ...messages.map((m) => ({ role: m.role, content: m.text })),
  ];
  let assistantText = "";
  try {
    const resp = await fetch("https://api.openai.com/v1/responses", {
      method: "POST",
      headers: {
        "content-type": "application/json",
        authorization: `Bearer ${OPENAI_API_KEY}`,
      },
      body: JSON.stringify({
        model: OPENAI_MODEL,
        input: openaiInput,
        tools: [{ type: "web_search" }],
        max_output_tokens: 1000,
      }),
    });
    if (!resp.ok) {
      console.error("openai_upstream_error", resp.status, await resp.text());
      return json({ error: "upstream_error" }, 502);
    }
    const payload = (await resp.json()) as ResponsesPayload;
    assistantText = extractText(payload);
  } catch (e) {
    console.error("openai_fetch_failed", e);
    return json({ error: "upstream_error" }, 502);
  }

  if (!assistantText) {
    return json({ error: "upstream_error" }, 502);
  }

  // 8) 성공한 경우에만 카운트 +1 (upsert 패턴)
  const newCount = usedToday + 1;
  const { error: upsertErr } = await adminClient
    .from("ai_chat_usage")
    .upsert(
      { user_id: user.id, day_kst: day, count: newCount, updated_at: new Date().toISOString() },
      { onConflict: "user_id,day_kst" }
    );
  if (upsertErr) console.error("ai_chat_usage_upsert_failed", upsertErr);

  return json({
    assistant: { text: assistantText },
    usage: { tier, usedToday: newCount, limit },
  });
});
```

> **이미지 추출은 클라가 한다** (parseAssistantText). Edge Function은 raw text만 반환.
> **카운트 +1은 race condition이 미세하게 있지만** 일일 한도 1~30 수준에선 무시 가능. 엄격 원자성이 필요하면 후속 plan에서 RPC로 옮긴다.

- [ ] **Step 3: TypeScript syntax sanity check (Deno parse)**

Run: `deno check supabase/functions/ai-chat/index.ts`
Expected: error 없음. (Deno가 없으면 supabase CLI의 deno로 실행하거나 step 4의 deploy 단계에서 검증.)

- [ ] **Step 4: 커밋**

```bash
git add supabase/functions/ai-chat/index.ts supabase/functions/ai-chat/deno.jsonc
git commit -m "feat(ai-chat): Edge Function 구현 - JWT/tier/한도/OpenAI 호출"
```

---

## Phase B: 클라이언트 코어 (types, pure utils, 네트워크, 스토어)

### Task B1: `types.ts`

**Files:**
- Create: `src/features/aiChat/types.ts`

- [ ] **Step 1: 작성**

```ts
// 클라가 다루는 채팅 메시지. id/createdAt은 클라 생성. 서버 전송 시엔 role/text만 보낸다.

export type ChatRole = "user" | "assistant";

export type ChatMessage = {
  id: string;
  role: ChatRole;
  text: string;
  createdAt: number;
  imageUrls?: string[]; // assistant 메시지 한정. 최대 4장.
  error?: string;       // assistant 응답이 실패했을 때 표시용 사유. (i18n key)
};

export type SendOutcome =
  | { kind: "ok"; tier: TierName; usedToday: number; limit: number }
  | { kind: "rate_limited"; tier: TierName; limit: number }
  | { kind: "network" }
  | { kind: "invalid_input" }
  | { kind: "upstream" }
  | { kind: "unknown" };

export type TierName = "free" | "premium" | "power";
```

- [ ] **Step 2: 커밋**

```bash
git add src/features/aiChat/types.ts
git commit -m "feat(ai-chat): types"
```

---

### Task B2: `parseAssistantText.ts` + 단위 테스트

**Files:**
- Create: `src/features/aiChat/parseAssistantText.ts`
- Create: `src/features/aiChat/parseAssistantText.test.ts`

- [ ] **Step 1: 테스트 먼저**

```ts
import { parseAssistantText } from "./parseAssistantText";

describe("parseAssistantText", () => {
  test("plain text → text only, no images", () => {
    const out = parseAssistantText("그냥 인사야 안녕");
    expect(out.text).toBe("그냥 인사야 안녕");
    expect(out.imageUrls).toEqual([]);
  });

  test("extracts https markdown image, removes token from text", () => {
    const out = parseAssistantText(
      "교토 가을 ![autumn](https://example.com/kyoto.jpg) 진짜 좋아"
    );
    expect(out.imageUrls).toEqual(["https://example.com/kyoto.jpg"]);
    expect(out.text).toBe("교토 가을  진짜 좋아");
  });

  test("ignores http (non-https)", () => {
    const out = parseAssistantText("![x](http://insecure.example/a.jpg)");
    expect(out.imageUrls).toEqual([]);
  });

  test("max 4 images, extras ignored", () => {
    const md = Array.from({ length: 6 })
      .map((_, i) => `![n${i}](https://h.example/${i}.jpg)`)
      .join(" ");
    const out = parseAssistantText(md);
    expect(out.imageUrls).toHaveLength(4);
  });

  test("dedupes same URL", () => {
    const out = parseAssistantText(
      "![a](https://x.example/a.jpg) ![b](https://x.example/a.jpg)"
    );
    expect(out.imageUrls).toEqual(["https://x.example/a.jpg"]);
  });

  test("malformed markdown ignored", () => {
    const out = parseAssistantText("![alt(https://x.example/a.jpg)");
    expect(out.imageUrls).toEqual([]);
    expect(out.text).toContain("![alt(https://x.example/a.jpg)");
  });

  test("trims excessive whitespace from text after stripping markdown", () => {
    const out = parseAssistantText(
      "사진 ![x](https://h.example/a.jpg)\n\n\n\n잘 봐"
    );
    expect(out.text).toBe("사진 \n\n잘 봐");
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npx jest src/features/aiChat/parseAssistantText.test.ts`
Expected: FAIL (모듈 없음)

- [ ] **Step 3: 구현**

```ts
const MAX_IMAGES = 4;
const IMG_RE = /!\[[^\]]*\]\((https:\/\/[^)\s]+)\)/g;

export type ParsedAssistant = {
  text: string;
  imageUrls: string[];
};

export function parseAssistantText(raw: string): ParsedAssistant {
  const urls: string[] = [];
  const seen = new Set<string>();
  // matchAll로 토큰을 모은 다음 텍스트에서 제거
  for (const m of raw.matchAll(IMG_RE)) {
    const url = m[1];
    if (seen.has(url)) continue;
    if (urls.length < MAX_IMAGES) {
      urls.push(url);
      seen.add(url);
    }
    // 4개 초과여도 본문 토큰은 모두 제거(노이즈 방지)
  }
  // 모든 매칭 토큰 제거. 연속 공백/개행이 과도하게 남으면 3개 이상은 2개로 줄인다.
  let text = raw.replace(IMG_RE, "");
  text = text.replace(/\n{3,}/g, "\n\n");
  return { text, imageUrls: urls };
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npx jest src/features/aiChat/parseAssistantText.test.ts`
Expected: 7 passed.

- [ ] **Step 5: 커밋**

```bash
git add src/features/aiChat/parseAssistantText.ts src/features/aiChat/parseAssistantText.test.ts
git commit -m "feat(ai-chat): parseAssistantText - 마크다운 이미지 추출 + 단위 테스트"
```

---

### Task B3: `aiChatStorage.ts` (AsyncStorage 어댑터)

**Files:**
- Create: `src/features/aiChat/aiChatStorage.ts`

**왜:** 메시지 sliding window를 사용자 단위로 영속화. 단순 read/write/clear.

- [ ] **Step 1: 작성**

```ts
import AsyncStorage from "@react-native-async-storage/async-storage";

import type { ChatMessage } from "./types";

const KEY_PREFIX = "ai_chat:";
export const MAX_MEMORY_MESSAGES = 10;

function key(userId: string): string {
  return KEY_PREFIX + userId;
}

export async function loadMessages(userId: string): Promise<ChatMessage[]> {
  const raw = await AsyncStorage.getItem(key(userId));
  if (!raw) return [];
  try {
    const parsed = JSON.parse(raw);
    if (!Array.isArray(parsed)) return [];
    return parsed as ChatMessage[];
  } catch {
    return [];
  }
}

export async function saveMessages(
  userId: string,
  messages: ChatMessage[]
): Promise<void> {
  const trimmed = messages.slice(-MAX_MEMORY_MESSAGES);
  await AsyncStorage.setItem(key(userId), JSON.stringify(trimmed));
}

export async function clearMessages(userId: string): Promise<void> {
  await AsyncStorage.removeItem(key(userId));
}
```

- [ ] **Step 2: 커밋**

```bash
git add src/features/aiChat/aiChatStorage.ts
git commit -m "feat(ai-chat): aiChatStorage - AsyncStorage 어댑터, 사용자별 키, 10개 윈도우"
```

---

### Task B4: `aiChatClient.ts` (Edge Function 호출)

**Files:**
- Create: `src/features/aiChat/aiChatClient.ts`

- [ ] **Step 1: 작성**

```ts
import { supabase } from "../../lib/supabase";

import { parseAssistantText } from "./parseAssistantText";
import type { ChatMessage, SendOutcome, TierName } from "./types";

type WireMessage = { role: "user" | "assistant"; text: string };
type WireResponse = {
  assistant?: { text: string };
  usage?: { tier: TierName; usedToday: number; limit: number };
  error?: string;
  tier?: TierName;
  limit?: number;
};

export type SendArgs = {
  history: ChatMessage[]; // 전송 전 sliding window
  newUserText: string;
};

export type SendResult = {
  outcome: SendOutcome;
  assistant: { text: string; imageUrls: string[] } | null;
};

function toWire(messages: ChatMessage[], newUserText: string): WireMessage[] {
  const arr: WireMessage[] = messages.map((m) => ({ role: m.role, text: m.text }));
  arr.push({ role: "user", text: newUserText });
  return arr;
}

export async function sendChat({ history, newUserText }: SendArgs): Promise<SendResult> {
  try {
    const { data, error } = await supabase.functions.invoke<WireResponse>("ai-chat", {
      body: { messages: toWire(history, newUserText) },
    });
    if (error) {
      // supabase.functions.invoke는 4xx/5xx도 error로 표면화한다. status는 context에 안 실리므로 body 우선.
      const body = (error as { context?: { body?: WireResponse } })?.context?.body;
      if (body?.error === "rate_limited" && body.tier && body.limit) {
        return {
          outcome: { kind: "rate_limited", tier: body.tier, limit: body.limit },
          assistant: null,
        };
      }
      if (body?.error === "invalid_input") {
        return { outcome: { kind: "invalid_input" }, assistant: null };
      }
      if (body?.error === "upstream_error") {
        return { outcome: { kind: "upstream" }, assistant: null };
      }
      return { outcome: { kind: "unknown" }, assistant: null };
    }
    if (!data?.assistant?.text || !data.usage) {
      return { outcome: { kind: "upstream" }, assistant: null };
    }
    const parsed = parseAssistantText(data.assistant.text);
    return {
      outcome: {
        kind: "ok",
        tier: data.usage.tier,
        usedToday: data.usage.usedToday,
        limit: data.usage.limit,
      },
      assistant: parsed,
    };
  } catch {
    return { outcome: { kind: "network" }, assistant: null };
  }
}
```

- [ ] **Step 2: 커밋**

```bash
git add src/features/aiChat/aiChatClient.ts
git commit -m "feat(ai-chat): aiChatClient - Edge Function 호출 + 결과 정규화"
```

---

### Task B5: `aiChatStore.ts` (Zustand)

**Files:**
- Create: `src/features/aiChat/aiChatStore.ts`

**왜:** UI는 이 스토어만 본다. userId 변경 시 메모리 reload, 전송, 비우기, 한도 상태.

- [ ] **Step 1: 작성**

```ts
import { create } from "zustand";

import { sendChat } from "./aiChatClient";
import { clearMessages, loadMessages, saveMessages } from "./aiChatStorage";
import type { ChatMessage, TierName } from "./types";

function uuid(): string {
  // expo-crypto가 따로 없으니 짧은 랜덤. 충돌 무시 가능 수준.
  return Math.random().toString(36).slice(2) + Date.now().toString(36);
}

type RateLimitInfo = { tier: TierName; limit: number };

type State = {
  loadedUserId: string | null;
  messages: ChatMessage[];
  isSending: boolean;
  rateLimit: RateLimitInfo | null;

  hydrate: (userId: string) => Promise<void>;
  send: (text: string) => Promise<void>;
  clear: () => Promise<void>;
};

export const useAiChatStore = create<State>((set, get) => ({
  loadedUserId: null,
  messages: [],
  isSending: false,
  rateLimit: null,

  hydrate: async (userId) => {
    if (get().loadedUserId === userId) return;
    const messages = await loadMessages(userId);
    set({ loadedUserId: userId, messages, rateLimit: null });
  },

  send: async (text) => {
    const userId = get().loadedUserId;
    if (!userId || get().isSending) return;
    const trimmed = text.trim();
    if (!trimmed) return;

    const userMessage: ChatMessage = {
      id: uuid(),
      role: "user",
      text: trimmed,
      createdAt: Date.now(),
    };
    const historyForCall = get().messages;
    const after = [...historyForCall, userMessage];
    set({ messages: after, isSending: true });
    await saveMessages(userId, after);

    const { outcome, assistant } = await sendChat({
      history: historyForCall,
      newUserText: trimmed,
    });

    if (outcome.kind === "ok" && assistant) {
      const aiMsg: ChatMessage = {
        id: uuid(),
        role: "assistant",
        text: assistant.text,
        imageUrls: assistant.imageUrls.length ? assistant.imageUrls : undefined,
        createdAt: Date.now(),
      };
      const next = [...after, aiMsg];
      set({ messages: next, isSending: false, rateLimit: null });
      await saveMessages(userId, next);
      return;
    }

    if (outcome.kind === "rate_limited") {
      // 사용자 메시지는 유지하되, 어시스턴트 자리에 에러 버블을 추가
      const aiMsg: ChatMessage = {
        id: uuid(),
        role: "assistant",
        text: "",
        error: `aiChat.error.rateLimited.${outcome.tier}`,
        createdAt: Date.now(),
      };
      const next = [...after, aiMsg];
      set({
        messages: next,
        isSending: false,
        rateLimit: { tier: outcome.tier, limit: outcome.limit },
      });
      await saveMessages(userId, next);
      return;
    }

    const errKey =
      outcome.kind === "network"
        ? "aiChat.error.network"
        : outcome.kind === "invalid_input"
        ? "aiChat.error.invalidInput"
        : "aiChat.error.generic";
    const aiMsg: ChatMessage = {
      id: uuid(),
      role: "assistant",
      text: "",
      error: errKey,
      createdAt: Date.now(),
    };
    const next = [...after, aiMsg];
    set({ messages: next, isSending: false });
    await saveMessages(userId, next);
  },

  clear: async () => {
    const userId = get().loadedUserId;
    if (!userId) return;
    await clearMessages(userId);
    set({ messages: [], rateLimit: null });
  },
}));
```

- [ ] **Step 2: 커밋**

```bash
git add src/features/aiChat/aiChatStore.ts
git commit -m "feat(ai-chat): aiChatStore - Zustand 메시지/전송/비우기/한도 상태"
```

---

### Task B6: 로그아웃 시 AI 채팅 메모리 정리

**Files:**
- Modify: `src/features/auth/authStore.ts`

**왜:** signOut 시 AsyncStorage의 `ai_chat:<userId>` 항목을 지워 다음 사용자가 이전 사용자 대화를 보지 못하게 한다.

- [ ] **Step 1: 수정**

기존 `signOut`:

```ts
  signOut: async () => {
    await signOutFromGoogle();
    await supabase.auth.signOut();
  },
```

→

```ts
  signOut: async () => {
    const userId = useAuthStore.getState().user?.id;
    if (userId) {
      try {
        await clearMessages(userId);
      } catch {
        // 메모리 정리 실패는 로그아웃 자체를 막지 않는다.
      }
    }
    await signOutFromGoogle();
    await supabase.auth.signOut();
  },
```

`authStore.ts` 상단 import에 추가:

```ts
import { clearMessages } from "../aiChat/aiChatStorage";
```

- [ ] **Step 2: 빌드 체크**

Run: `npx tsc --noEmit`
Expected: 에러 없음.

- [ ] **Step 3: 커밋**

```bash
git add src/features/auth/authStore.ts
git commit -m "feat(ai-chat): 로그아웃 시 AI 채팅 메모리 정리"
```

---

## Phase C: UI 컴포넌트

### Task C1: `AiChatBubble.tsx`

**Files:**
- Create: `src/components/AiChat/AiChatBubble.tsx`

- [ ] **Step 1: 작성**

```tsx
import { useMemo } from "react";
import { useTranslation } from "react-i18next";
import {
  Image,
  Pressable,
  StyleSheet,
  Text,
  View,
} from "react-native";

import { useTheme } from "../../theme/themeStore";
import type { ChatMessage } from "../../features/aiChat/types";

type Props = {
  message: ChatMessage;
  onImagePress: (url: string) => void;
};

export default function AiChatBubble({ message, onImagePress }: Props) {
  const theme = useTheme();
  const { t } = useTranslation();
  const styles = useMemo(() => makeStyles(theme), [theme]);

  const isUser = message.role === "user";
  const text = message.error ? t(message.error) : message.text;

  return (
    <View style={[styles.row, isUser ? styles.rowUser : styles.rowAssistant]}>
      <View
        style={[
          styles.bubble,
          isUser ? styles.bubbleUser : styles.bubbleAssistant,
          message.error ? styles.bubbleError : null,
        ]}
      >
        {text ? (
          <Text style={[styles.text, isUser ? styles.textUser : styles.textAssistant]}>
            {text}
          </Text>
        ) : null}
        {message.imageUrls && message.imageUrls.length > 0 ? (
          <View style={styles.imageGrid}>
            {message.imageUrls.map((url) => (
              <Pressable
                key={url}
                style={styles.imageWrap}
                onPress={() => onImagePress(url)}
              >
                <Image
                  source={{ uri: url }}
                  style={styles.image}
                  resizeMode="cover"
                />
              </Pressable>
            ))}
          </View>
        ) : null}
      </View>
    </View>
  );
}

function makeStyles(theme: ReturnType<typeof useTheme>) {
  return StyleSheet.create({
    row: {
      paddingHorizontal: 12,
      paddingVertical: 4,
      flexDirection: "row",
    },
    rowUser: { justifyContent: "flex-end" },
    rowAssistant: { justifyContent: "flex-start" },
    bubble: {
      maxWidth: "82%",
      borderRadius: 14,
      paddingHorizontal: 12,
      paddingVertical: 8,
    },
    bubbleUser: {
      backgroundColor: theme.accent,
      borderBottomRightRadius: 4,
    },
    bubbleAssistant: {
      backgroundColor: theme.cardBg,
      borderBottomLeftRadius: 4,
    },
    bubbleError: { opacity: 0.85 },
    text: { fontSize: 15, lineHeight: 21 },
    textUser: { color: theme.accentOn },
    textAssistant: { color: theme.textPrimary },
    imageGrid: {
      marginTop: 6,
      flexDirection: "row",
      flexWrap: "wrap",
      gap: 4,
    },
    imageWrap: {
      width: "49%",
      aspectRatio: 1,
      borderRadius: 12,
      overflow: "hidden",
    },
    image: { width: "100%", height: "100%" },
  });
}
```

> **테마 키 확인:** `accent`, `accentOn`, `cardBg`, `textPrimary`가 themeStore에 있는지 확인. 없는 키는 가장 가까운 키(예: `accent`가 없다면 `homeBg`/대비 계열)로 대체. Step 2에서 검증.

- [ ] **Step 2: 테마 키 확인**

Run: `grep -n "accent\|cardBg\|accentOn" src/theme/themeStore.ts | head -10`
없는 키가 있으면 그 자리에 가장 가까운 기존 키를 사용하도록 위 코드를 즉시 수정한다.

- [ ] **Step 3: 빌드 체크**

Run: `npx tsc --noEmit`
Expected: 에러 없음.

- [ ] **Step 4: 커밋**

```bash
git add src/components/AiChat/AiChatBubble.tsx
git commit -m "feat(ai-chat): AiChatBubble - 말풍선 + 이미지 그리드"
```

---

### Task C2: `AiChatList.tsx`

**Files:**
- Create: `src/components/AiChat/AiChatList.tsx`

- [ ] **Step 1: 작성**

```tsx
import { useMemo } from "react";
import { FlatList, StyleSheet, View } from "react-native";

import type { ChatMessage } from "../../features/aiChat/types";
import AiChatBubble from "./AiChatBubble";

type Props = {
  messages: ChatMessage[];
  onImagePress: (url: string) => void;
};

export default function AiChatList({ messages, onImagePress }: Props) {
  // inverted FlatList엔 역순으로 넘긴다.
  const reversed = useMemo(() => [...messages].reverse(), [messages]);

  return (
    <FlatList
      data={reversed}
      inverted
      keyExtractor={(m) => m.id}
      renderItem={({ item }) => (
        <AiChatBubble message={item} onImagePress={onImagePress} />
      )}
      contentContainerStyle={styles.content}
      keyboardDismissMode="interactive"
      ItemSeparatorComponent={() => <View style={styles.sep} />}
    />
  );
}

const styles = StyleSheet.create({
  content: { paddingVertical: 8 },
  sep: { height: 2 },
});
```

- [ ] **Step 2: 커밋**

```bash
git add src/components/AiChat/AiChatList.tsx
git commit -m "feat(ai-chat): AiChatList - inverted FlatList"
```

---

### Task C3: `AiChatComposer.tsx`

**Files:**
- Create: `src/components/AiChat/AiChatComposer.tsx`

- [ ] **Step 1: 작성**

```tsx
import { Send } from "lucide-react-native";
import { forwardRef, useImperativeHandle, useMemo, useState } from "react";
import { useTranslation } from "react-i18next";
import {
  ActivityIndicator,
  Pressable,
  StyleSheet,
  Text,
  TextInput,
  View,
} from "react-native";

import { useTheme } from "../../theme/themeStore";

const MAX = 500;

export type AiChatComposerHandle = {
  setText: (next: string) => void;
};

type Props = {
  isSending: boolean;
  disabled: boolean;
  onSend: (text: string) => void;
};

const AiChatComposer = forwardRef<AiChatComposerHandle, Props>(function AiChatComposer(
  { isSending, disabled, onSend },
  ref
) {
  const theme = useTheme();
  const { t } = useTranslation();
  const styles = useMemo(() => makeStyles(theme), [theme]);
  const [text, setText] = useState("");

  useImperativeHandle(ref, () => ({ setText: (n: string) => setText(n) }), []);

  const trimmedLen = text.trim().length;
  const sendDisabled = disabled || isSending || trimmedLen === 0;

  const handleSend = () => {
    if (sendDisabled) return;
    const t = text.trim();
    setText("");
    onSend(t);
  };

  return (
    <View style={styles.root}>
      <View style={styles.inputRow}>
        <TextInput
          value={text}
          onChangeText={setText}
          placeholder={t("aiChat.composerPlaceholder")}
          placeholderTextColor={theme.textSecondary}
          editable={!disabled}
          multiline
          maxLength={MAX}
          style={styles.input}
        />
        <Pressable
          onPress={handleSend}
          disabled={sendDisabled}
          style={({ pressed }) => [
            styles.sendBtn,
            sendDisabled ? styles.sendBtnDisabled : null,
            pressed && !sendDisabled ? styles.sendBtnPressed : null,
          ]}
          accessibilityLabel={t("aiChat.send")}
        >
          {isSending ? (
            <ActivityIndicator color={theme.accentOn} />
          ) : (
            <Send size={20} color={theme.accentOn} />
          )}
        </Pressable>
      </View>
      <Text style={styles.counter}>
        {t("aiChat.composerCounter", { n: text.length })}
      </Text>
    </View>
  );
});

export default AiChatComposer;

function makeStyles(theme: ReturnType<typeof useTheme>) {
  return StyleSheet.create({
    root: {
      borderTopWidth: StyleSheet.hairlineWidth,
      borderTopColor: theme.divider,
      paddingHorizontal: 12,
      paddingTop: 8,
      paddingBottom: 8,
      backgroundColor: theme.homeBg,
    },
    inputRow: {
      flexDirection: "row",
      alignItems: "flex-end",
      gap: 8,
    },
    input: {
      flex: 1,
      minHeight: 40,
      maxHeight: 120,
      paddingHorizontal: 12,
      paddingVertical: 8,
      borderRadius: 18,
      backgroundColor: theme.cardBg,
      color: theme.textPrimary,
      fontSize: 15,
    },
    sendBtn: {
      width: 40,
      height: 40,
      borderRadius: 20,
      alignItems: "center",
      justifyContent: "center",
      backgroundColor: theme.accent,
    },
    sendBtnDisabled: { opacity: 0.4 },
    sendBtnPressed: { opacity: 0.8 },
    counter: {
      marginTop: 4,
      alignSelf: "flex-end",
      fontSize: 11,
      color: theme.textSecondary,
    },
  });
}
```

- [ ] **Step 2: 빌드 체크**

Run: `npx tsc --noEmit`

- [ ] **Step 3: 커밋**

```bash
git add src/components/AiChat/AiChatComposer.tsx
git commit -m "feat(ai-chat): AiChatComposer - 500자 제한 + 카운터 + 전송 버튼"
```

---

### Task C4: `AiChatEmptyState.tsx`

**Files:**
- Create: `src/components/AiChat/AiChatEmptyState.tsx`

- [ ] **Step 1: 작성**

```tsx
import { useMemo } from "react";
import { useTranslation } from "react-i18next";
import { Pressable, StyleSheet, Text, View } from "react-native";

import { useTheme } from "../../theme/themeStore";

type Props = {
  onPickExample: (text: string) => void;
};

const KEYS = [
  "aiChat.exampleNextDestination",
  "aiChat.exampleSafety",
  "aiChat.examplePattern",
] as const;

export default function AiChatEmptyState({ onPickExample }: Props) {
  const theme = useTheme();
  const { t } = useTranslation();
  const styles = useMemo(() => makeStyles(theme), [theme]);

  return (
    <View style={styles.root}>
      <Text style={styles.heading}>{t("aiChat.empty.heading")}</Text>
      <Text style={styles.sub}>{t("aiChat.empty.subheading")}</Text>
      <View style={styles.chips}>
        {KEYS.map((k) => {
          const label = t(k);
          return (
            <Pressable
              key={k}
              style={({ pressed }) => [
                styles.chip,
                pressed ? styles.chipPressed : null,
              ]}
              onPress={() => onPickExample(label)}
            >
              <Text style={styles.chipText}>{label}</Text>
            </Pressable>
          );
        })}
      </View>
    </View>
  );
}

function makeStyles(theme: ReturnType<typeof useTheme>) {
  return StyleSheet.create({
    root: {
      flex: 1,
      alignItems: "center",
      justifyContent: "center",
      paddingHorizontal: 24,
      gap: 8,
    },
    heading: {
      fontSize: 18,
      fontWeight: "600",
      color: theme.textPrimary,
      textAlign: "center",
    },
    sub: {
      fontSize: 14,
      color: theme.textSecondary,
      textAlign: "center",
      marginBottom: 16,
    },
    chips: { width: "100%", gap: 8 },
    chip: {
      paddingHorizontal: 14,
      paddingVertical: 10,
      borderRadius: 20,
      backgroundColor: theme.cardBg,
      borderWidth: StyleSheet.hairlineWidth,
      borderColor: theme.divider,
    },
    chipPressed: { opacity: 0.7 },
    chipText: { color: theme.textPrimary, fontSize: 14 },
  });
}
```

- [ ] **Step 2: 커밋**

```bash
git add src/components/AiChat/AiChatEmptyState.tsx
git commit -m "feat(ai-chat): AiChatEmptyState - 빈 상태 + 예시 칩 3개"
```

---

### Task C5: `UsageLimitBanner.tsx`

**Files:**
- Create: `src/components/AiChat/UsageLimitBanner.tsx`

- [ ] **Step 1: 작성**

```tsx
import { useMemo } from "react";
import { useTranslation } from "react-i18next";
import { StyleSheet, Text, View } from "react-native";

import { useTheme } from "../../theme/themeStore";
import type { TierName } from "../../features/aiChat/types";

type Props = {
  tier: TierName;
  limit: number;
};

export default function UsageLimitBanner({ tier, limit }: Props) {
  const theme = useTheme();
  const { t } = useTranslation();
  const styles = useMemo(() => makeStyles(theme), [theme]);
  return (
    <View style={styles.root}>
      <Text style={styles.text}>
        {t(`aiChat.error.rateLimited.${tier}`, { limit })}
      </Text>
    </View>
  );
}

function makeStyles(theme: ReturnType<typeof useTheme>) {
  return StyleSheet.create({
    root: {
      paddingHorizontal: 12,
      paddingVertical: 8,
      backgroundColor: theme.dangerBg ?? "#5a1f24",
      borderTopWidth: StyleSheet.hairlineWidth,
      borderTopColor: theme.divider,
    },
    text: {
      color: theme.dangerOn ?? "#ffe5e7",
      fontSize: 13,
      textAlign: "center",
    },
  });
}
```

> `dangerBg`/`dangerOn`이 themeStore에 없으면 fallback 색만 사용된다(?? 우항). 추가 가능하면 추가.

- [ ] **Step 2: 커밋**

```bash
git add src/components/AiChat/UsageLimitBanner.tsx
git commit -m "feat(ai-chat): UsageLimitBanner - 한도 초과 안내 배너"
```

---

### Task C6: `index.ts` (barrel export)

**Files:**
- Create: `src/components/AiChat/index.ts`

- [ ] **Step 1: 작성**

```ts
export { default as AiChatBubble } from "./AiChatBubble";
export { default as AiChatList } from "./AiChatList";
export { default as AiChatComposer } from "./AiChatComposer";
export type { AiChatComposerHandle } from "./AiChatComposer";
export { default as AiChatEmptyState } from "./AiChatEmptyState";
export { default as UsageLimitBanner } from "./UsageLimitBanner";
```

- [ ] **Step 2: 커밋**

```bash
git add src/components/AiChat/index.ts
git commit -m "feat(ai-chat): components barrel export"
```

---

## Phase D: 통합 (i18n + 화면)

### Task D1: i18n 키 추가 (ko, en 정본)

**Files:**
- Modify: `src/i18n/locales/ko.json`
- Modify: `src/i18n/locales/en.json`

- [ ] **Step 1: ko.json 최상단 객체 안에 추가** (적절한 위치, 알파벳 순 권장)

```json
"aiChat": {
  "title": "AI",
  "headerClear": "대화 비우기",
  "composerPlaceholder": "여행 친구에게 물어보세요",
  "composerCounter": "{{n}}/500",
  "send": "보내기",
  "thinking": "타이핑 중…",
  "empty": {
    "heading": "여행 친구에게 물어봐",
    "subheading": "네 여행 데이터로 답해줄게"
  },
  "exampleNextDestination": "다음 여행지는 어디가 좋을까?",
  "exampleSafety": "요즘 안전한 여행지 어디야?",
  "examplePattern": "내 여행 스타일 어떤 거 같아?",
  "error": {
    "generic": "잠깐 문제가 생겼어. 다시 시도해줘.",
    "network": "네트워크가 불안정해. 다시 시도해줘.",
    "invalidInput": "메시지를 다시 확인해줘.",
    "imageLoadFailed": "이미지를 불러올 수 없어요",
    "rateLimited": {
      "free": "오늘 무료 한도({{limit}}회)를 다 썼어. 등급을 올리면 더 많이 쓸 수 있어.",
      "premium": "오늘 한도({{limit}}회)를 다 썼어. 내일 자정(KST)에 다시 채워져.",
      "power": "오늘 한도({{limit}}회)를 다 썼어. 내일 자정(KST)에 다시 채워져."
    }
  },
  "confirmClear": {
    "title": "대화 비우기",
    "body": "지금까지 대화를 모두 지울까?",
    "confirm": "지우기",
    "cancel": "취소"
  }
}
```

- [ ] **Step 2: en.json — 동일 구조의 영문**

```json
"aiChat": {
  "title": "AI",
  "headerClear": "Clear chat",
  "composerPlaceholder": "Ask your travel buddy",
  "composerCounter": "{{n}}/500",
  "send": "Send",
  "thinking": "Thinking…",
  "empty": {
    "heading": "Ask your travel buddy",
    "subheading": "I'll answer using your travel data"
  },
  "exampleNextDestination": "Where should I go next?",
  "exampleSafety": "Where's safe to travel right now?",
  "examplePattern": "What's my travel vibe?",
  "error": {
    "generic": "Something went off. Try again?",
    "network": "Network looks shaky. Try again?",
    "invalidInput": "Mind checking that message?",
    "imageLoadFailed": "Couldn't load image",
    "rateLimited": {
      "free": "You've used today's free quota ({{limit}}). Upgrade to chat more.",
      "premium": "You've used today's quota ({{limit}}). Resets at midnight KST.",
      "power": "You've used today's quota ({{limit}}). Resets at midnight KST."
    }
  },
  "confirmClear": {
    "title": "Clear chat",
    "body": "Wipe everything we've said so far?",
    "confirm": "Clear",
    "cancel": "Cancel"
  }
}
```

- [ ] **Step 3: 커밋**

```bash
git add src/i18n/locales/ko.json src/i18n/locales/en.json
git commit -m "feat(ai-chat): i18n - aiChat.* (ko/en 친구체)"
```

---

### Task D2: i18n 영어값 미러링 (ja, zh-CN, zh-TW, es, de, fr, it, ru)

**Files:** (8개)
- Modify: `src/i18n/locales/ja.json`
- Modify: `src/i18n/locales/zh-CN.json`
- Modify: `src/i18n/locales/zh-TW.json`
- Modify: `src/i18n/locales/es.json`
- Modify: `src/i18n/locales/de.json`
- Modify: `src/i18n/locales/fr.json`
- Modify: `src/i18n/locales/it.json`
- Modify: `src/i18n/locales/ru.json`

**왜:** 키 누락 방지. 각 파일에 D1의 **en.json과 동일한 `aiChat` 블록을 그대로** 넣어둔다(향후 native 검토 PR에서 친구체로 다듬는다). LLM 응답 자체의 친구체 톤은 system prompt가 모든 언어에서 처리하므로 UI 라벨만 영어로 노출되어도 사용성에 큰 결함은 없다.

- [ ] **Step 1: 각 파일의 최상단 객체 안에 D1 step 2의 `aiChat` 블록을 그대로 붙여넣기**

> 8개 파일 모두 같은 위치, 같은 내용. 각 언어 파일의 기존 키 순서를 따른다.

- [ ] **Step 2: 빌드 체크**

Run: `npx tsc --noEmit`

- [ ] **Step 3: 커밋**

```bash
git add src/i18n/locales/ja.json src/i18n/locales/zh-CN.json src/i18n/locales/zh-TW.json src/i18n/locales/es.json src/i18n/locales/de.json src/i18n/locales/fr.json src/i18n/locales/it.json src/i18n/locales/ru.json
git commit -m "feat(ai-chat): i18n - 8개 언어에 aiChat.* 영어값 미러링 (native 검토 대기)"
```

---

### Task D3: `AiScreen.tsx` 교체

**Files:**
- Modify: `src/screens/AiScreen.tsx` (전체 교체)

- [ ] **Step 1: 작성 (전체 교체)**

```tsx
import { Trash2 } from "lucide-react-native";
import { useEffect, useMemo, useRef, useState } from "react";
import { useTranslation } from "react-i18next";
import {
  Alert,
  KeyboardAvoidingView,
  Platform,
  Pressable,
  StyleSheet,
  Text,
  View,
} from "react-native";

import {
  AiChatComposer,
  AiChatEmptyState,
  AiChatList,
  UsageLimitBanner,
  type AiChatComposerHandle,
} from "../components/AiChat";
import { useAiChatStore } from "../features/aiChat/aiChatStore";
import { useAuthStore } from "../features/auth/authStore";
import { useScreenBottomInset } from "../hooks/useScreenInsets";
import { useTheme } from "../theme/themeStore";

export default function AiScreen() {
  const theme = useTheme();
  const { t } = useTranslation();
  const styles = useMemo(() => makeStyles(theme), [theme]);
  const bottomInset = useScreenBottomInset();

  const userId = useAuthStore((s) => s.user?.id ?? null);
  const messages = useAiChatStore((s) => s.messages);
  const isSending = useAiChatStore((s) => s.isSending);
  const rateLimit = useAiChatStore((s) => s.rateLimit);
  const hydrate = useAiChatStore((s) => s.hydrate);
  const send = useAiChatStore((s) => s.send);
  const clear = useAiChatStore((s) => s.clear);

  const composerRef = useRef<AiChatComposerHandle>(null);

  useEffect(() => {
    if (userId) void hydrate(userId);
  }, [userId, hydrate]);

  const onClear = () => {
    if (messages.length === 0) return;
    Alert.alert(
      t("aiChat.confirmClear.title"),
      t("aiChat.confirmClear.body"),
      [
        { text: t("aiChat.confirmClear.cancel"), style: "cancel" },
        {
          text: t("aiChat.confirmClear.confirm"),
          style: "destructive",
          onPress: () => void clear(),
        },
      ]
    );
  };

  const onImagePress = (_url: string) => {
    // TODO: 차후 ImageDetailScreen에 url을 받게 확장. 현재는 noop.
  };

  return (
    <KeyboardAvoidingView
      style={styles.root}
      behavior={Platform.OS === "ios" ? "padding" : undefined}
    >
      <View style={styles.header}>
        <Text style={styles.title}>{t("aiChat.title")}</Text>
        <Pressable
          onPress={onClear}
          style={({ pressed }) => [styles.clearBtn, pressed ? styles.clearBtnPressed : null]}
          accessibilityLabel={t("aiChat.headerClear")}
          hitSlop={10}
        >
          <Trash2 size={20} color={theme.textSecondary} />
        </Pressable>
      </View>

      <View style={styles.body}>
        {messages.length === 0 ? (
          <AiChatEmptyState
            onPickExample={(text) => composerRef.current?.setText(text)}
          />
        ) : (
          <AiChatList messages={messages} onImagePress={onImagePress} />
        )}
      </View>

      {rateLimit ? (
        <UsageLimitBanner tier={rateLimit.tier} limit={rateLimit.limit} />
      ) : null}

      <View style={{ paddingBottom: bottomInset }}>
        <AiChatComposer
          ref={composerRef}
          isSending={isSending}
          disabled={!!rateLimit}
          onSend={(text) => void send(text)}
        />
      </View>
    </KeyboardAvoidingView>
  );
}

function makeStyles(theme: ReturnType<typeof useTheme>) {
  return StyleSheet.create({
    root: { flex: 1, backgroundColor: theme.homeBg },
    header: {
      flexDirection: "row",
      alignItems: "center",
      justifyContent: "space-between",
      paddingHorizontal: 16,
      paddingVertical: 12,
      borderBottomWidth: StyleSheet.hairlineWidth,
      borderBottomColor: theme.divider,
    },
    title: { fontSize: 18, fontWeight: "600", color: theme.textPrimary },
    clearBtn: { padding: 4, borderRadius: 8 },
    clearBtnPressed: { opacity: 0.6 },
    body: { flex: 1 },
  });
}
```

- [ ] **Step 2: 빌드 체크**

Run: `npx tsc --noEmit`
Expected: 에러 없음. (테마 키 부재로 에러 나면 그 자리에서 가까운 기존 키로 치환.)

- [ ] **Step 3: 커밋**

```bash
git add src/screens/AiScreen.tsx
git commit -m "feat(ai-chat): AiScreen 실제 채팅 화면으로 교체"
```

---

### Task D4: 배포 체크리스트 항목 추가

**Files:**
- Modify: `docs/배포-체크리스트.md`

- [ ] **Step 1: 문서 끝부분에 신규 섹션 추가**

문서 끝(혹은 가장 적절한 위치)에 다음을 append:

```markdown
---

## AI 채팅 (`ai-chat` Edge Function)

릴리스 전 확인:

- [ ] `OPENAI_API_KEY` Supabase secret 등록됨 (`supabase secrets list`로 확인)
- [ ] (선택) `OPENAI_MODEL` secret 설정 — 미설정 시 기본 `gpt-5.4-mini`
- [ ] `supabase/migrations/20260508120000_ai_chat_usage.sql` 적용됨 (`supabase db push` 또는 Dashboard SQL editor)
- [ ] `supabase functions deploy ai-chat` 완료
- [ ] OpenAI 콘솔에 월별 hard limit 설정됨 (사고 시 청구서 폭발 방지)
- [ ] 테스트 계정으로 무료(tier 0) 1회 응답 → 2회째 429 확인
- [ ] tier 1로 토글 후 10회 한도 정상 동작 확인 (dev에서만 직접 update 가능, 운영 시 §1과 동일하게 잠가야 함)

```

- [ ] **Step 2: 커밋**

```bash
git add docs/배포-체크리스트.md
git commit -m "docs: AI 채팅 운영 체크리스트 추가"
```

---

## Phase E: 검증

### Task E1: 전체 타입 체크 + 테스트

- [ ] **Step 1: 타입 체크**

Run: `npx tsc --noEmit`
Expected: 에러 없음.

- [ ] **Step 2: 클라 단위 테스트**

Run: `npx jest`
Expected: parseAssistantText 테스트 7개 + 기존 테스트들 모두 PASS.

- [ ] **Step 3: Edge Function Deno 테스트 (deno 가용 시)**

Run: `cd supabase/functions/ai-chat && deno test`
Expected: 11 passed (tripStats 5 + systemPrompt 6).

> deno 미설치면 step 3는 건너뛰고 통합 검증(Task E2)에서 동작 검증.

---

### Task E2: 사용자 환경에서 통합 검증 가이드 (사용자 작업)

> 이 task는 코드 변경이 아닌 **사용자가 직접 수행하는 검증** 절차다. plan에 명시해두면 release 시점에 빠뜨리지 않는다.

**전제:**
- supabase CLI 설치 (`brew install supabase/tap/supabase`)
- 프로젝트 link (`supabase link --project-ref <REF>`)

**절차:**

1. **마이그레이션 적용**
   - `supabase db push` (또는 Dashboard SQL Editor에 마이그레이션 SQL 붙여넣기)
2. **Secret 등록**
   - `supabase secrets set OPENAI_API_KEY=sk-...`
3. **Edge Function 배포**
   - `supabase functions deploy ai-chat`
4. **앱 빌드 후 디바이스에서**
   - 로그인 (Google/Apple)
   - AI 탭 진입 → 빈 상태 확인
   - 예시 칩 1번 탭 → 입력창 채워짐 확인
   - 전송 → "타이핑 중…" → 응답 도착 확인
   - 1회만 free 사용자에게 허용되므로, 2번째 메시지 시 429 한도 초과 배너 확인
   - dev: `await supabase.from("users").update({ tier: 2 }).eq("id", user.id)`로 power 변경, 30회까지 OK 확인
   - 응답에 markdown 이미지 URL 있는 질문(예: "교토 단풍 사진 보여줄 수 있어?") → inline 이미지 렌더 확인
   - "대화 비우기" → 메모리 비워짐 확인
   - 로그아웃 → 다른 계정 로그인 → 빈 상태 확인 (이전 사용자 대화 안 보임)
   - 다크모드 토글하며 시각적 검증

---

## 자체 검증 (이 plan 작성자가 작성 후 self-review)

- [x] spec §1~14 각 요구사항이 task로 매핑됨
  - §1 목적: 전체 plan
  - §2 사용자 흐름: D3
  - §3 아키텍처: A1~A4 + B1~B5
  - §4 데이터 모델: B1, B3, A1
  - §5 UI: C1~C6, D3
  - §6 Edge Function: A4
  - §7 tier 한도: A4 + user-tier doc(이미 commit)
  - §8 가드: A4 + C3 (500자 maxLength)
  - §9 i18n: D1, D2
  - §10 에지케이스: A4(서버), B5(클라 에러 분기), D3(빈 상태/네트워크 표시)
  - §11 키 운영: D4 (배포 체크리스트)
  - §12 분석: 명시적으로 범위 밖
  - §13 v2: doc 그대로
  - §14 작업 분리: 본 plan
- [x] 모든 task에 실제 코드 포함, "TBD" 없음
- [x] 함수/타입 이름 일관성: `ChatMessage`, `TierName`, `aggregateCountryStats`, `sortTripsForPrompt`, `buildSystemPrompt`, `parseAssistantText`, `loadMessages`/`saveMessages`/`clearMessages`, `sendChat`, `useAiChatStore` — 동일하게 사용
- [x] 파일 경로 모두 절대 경로 + 상대 경로 명시
